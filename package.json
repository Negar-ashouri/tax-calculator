{
  "name": "tax-calculator-root",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "lint": "cd client && npm run lint",
    "test": "cd client && npm test"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^13.0.0",
    "prettier": "^3.6.2"
  },
  "lint-staged": {
    "*.{js,ts,tsx,json,css,md}": [
      "prettier --write"
    ],
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix"
    ]
  }
}
